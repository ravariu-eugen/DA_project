kind: PodChaos
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: kafka
  name: pod-failure-3
  annotations:
    experiment.chaos-mesh.org/pause: 'true'
spec:
  selector:
    namespaces:
      - kafka
  mode: fixed
  value: '3'
  action: pod-failure
