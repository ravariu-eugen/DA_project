<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Define a console appender for general logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} || [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define a console appender for error logs with a different layout -->
    <appender name="ERROR_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - ERROR: %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
    </appender>

    <!-- Turn off INFO logging for Kafka libraries -->
    <logger name="org.apache.kafka" level="WARN"/>

    <!-- Set the root logger level and attach the appenders -->
    <root level="debug">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ERROR_CONSOLE"/>
    </root>

</configuration>
